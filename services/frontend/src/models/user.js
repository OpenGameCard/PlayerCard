export default class User {
  constructor(username, email, password, agreements) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.agreements = agreements;
  }
}
